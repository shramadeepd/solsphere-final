# Core dependencies
requests>=2.28.0
psutil>=5.9.0

# Cross-platform system utilities
pywin32>=305; sys_platform == "win32"
pyobjc-framework-SystemConfiguration>=9.0; sys_platform == "darwin"

# Logging and configuration
python-dotenv>=0.19.0

# Development dependencies (optional)
pytest>=6.0
pytest-cov>=2.0
black>=21.0
flake8>=3.8
mypy>=0.800
